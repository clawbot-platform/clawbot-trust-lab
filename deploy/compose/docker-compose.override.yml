services:
  postgres:
    environment:
      TZ: UTC

  control-plane:
    environment:
      LOG_LEVEL: ${LOG_LEVEL}

  clawmem:
    environment:
      CLAWMEM_LOG_LEVEL: ${LOG_LEVEL}

  trust-lab:
    environment:
      LOG_LEVEL: ${LOG_LEVEL}
