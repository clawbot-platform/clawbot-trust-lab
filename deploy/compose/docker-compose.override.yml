services:
  # Local-development defaults for the core Version 1 stack.
  postgres:
    environment:
      TZ: UTC

  control-plane:
    environment:
      TZ: UTC

  clawmem:
    environment:
      TZ: UTC

  trust-lab:
    environment:
      TZ: UTC

  trust-lab-ui:
    environment:
      TZ: UTC
